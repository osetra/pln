```
CLI args → parser → CommandParams → commandFactory → Command.execute()
                                                          ↓
                                          caldavClient (read/create/update/delete)
                                          cacheManager
                                          analizator
                                          consolePrinter
```
